'use client';

import React from 'react';
import './NoProjects.css';

type NoProjectsProps = {};

const NoProjects = ({}: NoProjectsProps) => {
  return <section className="no-projects">Create a project to get started</section>;
};

export default NoProjects;
